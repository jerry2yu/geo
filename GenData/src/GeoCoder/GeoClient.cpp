#include "GeoClient.h"
GeoClient::GeoClient() : index(NULL)
{
}

GeoClient::~GeoClient()
{
}
#if 0
int main()
{
	GeoClient client;
	if ( client.open() )
	{
		client.close();
	}
}
#endif